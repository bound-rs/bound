{
  "name": "md2html",
  "version": "1.0.0",
  "description": "Render Markdown as a standalone HTML page (an example for bound)",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "md2html": "src/md2html.js"
  },
  "scripts": {
    "bundle": "node build.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "marked": "^18.0.14",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  },
  "engines": {
    "node": ">=20"
  }
}
